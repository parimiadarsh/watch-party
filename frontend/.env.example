# Optional: send /api through the Node middleware (see ../middleware)
# VITE_DEV_API_PROXY=http://localhost:3001

# Copy to .env.development (Vite loads this in npm run dev)
# VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
